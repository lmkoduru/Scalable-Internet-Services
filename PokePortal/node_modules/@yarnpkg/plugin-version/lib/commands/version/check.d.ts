import { BaseCommand } from '@yarnpkg/cli';
import { Usage } from 'clipanion';
export default class VersionCheckCommand extends BaseCommand {
    static paths: string[][];
    static usage: Usage;
    interactive: boolean | undefined;
    execute(): Promise<0 | 1 | undefined>;
    executeInteractive(): Promise<0 | 1 | undefined>;
    executeStandard(): Promise<0 | 1>;
}
