import { Project } from '@yarnpkg/core';
import * as constraintUtils from './constraintUtils';
export declare class ModernEngine implements constraintUtils.Engine {
    private project;
    constructor(project: Project);
    private createEnvironment;
    process(): Promise<constraintUtils.ProcessResult | null>;
}
